#include "cuda_runtime.h"
#include "device_launch_parameters.h"

#include <stdio.h>
/*
class testClass
{
public:
	__host__ testClass();
	__host__ ~testClass();
	__host__ cudaError_t addWithCuda(int *c, const int *a, const int *b, unsigned int size);

private:

};
*/